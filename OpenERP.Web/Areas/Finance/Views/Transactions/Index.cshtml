@model IEnumerable<OpenERP.Finance.Models.Entities.Transaction>
@{
    ViewData["Title"] = "凭证管理";
}
<div class="container-fluid">
    <h1 class="mt-4">凭证管理</h1>
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">首页</a></li>
        <li class="breadcrumb-item active">凭证管理</li>
    </ol>
    <div class="card mb-4">
        <div class="card-header d-flex justify-content-between align-items-center">
            <div>
                <i class="bi bi-receipt me-1"></i>
                凭证列表
            </div>
            <a asp-area="Finance" asp-controller="Transactions" asp-action="Create" class="btn btn-primary btn-sm">
                <i class="bi bi-plus-circle me-1"></i> 新建凭证
            </a>
        </div>
        <div class="card-body">
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>单号</th>
                        <th>日期</th>
                        <th>科目</th>
                        <th>金额</th>
                        <th>状态</th>
                    </tr>
                </thead>
                <tbody>
                @if (!Model.Any())
                {
                    <tr><td colspan="5" class="text-center">暂无数据</td></tr>
                }
                else
                {
                    foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.DocNumber</td>
                            <td>@item.DocDate.ToString("yyyy-MM-dd")</td>
                            <td>@item.Account?.Name</td>
                            <td>@item.Amount.ToString("F2")</td>
                            <td>@item.Status</td>
                        </tr>
                    }
                }
                </tbody>
            </table>
        </div>
    </div>
</div>
