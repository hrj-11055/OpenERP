@model IEnumerable<OpenERP.CRM.Models.Entities.Opportunity>
@{
    ViewData["Title"] = "销售机会管理";
}

<div class="container-fluid">
    <h1 class="mt-4">销售机会管理</h1>
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item"><a asp-area="" asp-controller="Home" asp-action="Index">首页</a></li>
        <li class="breadcrumb-item active">销售机会管理</li>
    </ol>
    <div class="card mb-4">
        <div class="card-header d-flex justify-content-between align-items-center">
            <div>
                <i class="bi bi-currency-dollar me-1"></i>
                销售机会列表
            </div>
            <a asp-area="CRM" asp-controller="Opportunities" asp-action="Create" class="btn btn-primary btn-sm">
                <i class="bi bi-plus-circle me-1"></i> 新建机会
            </a>
        </div>
        <div class="card-body">
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>名称</th>
                        <th>客户名称</th>
                        <th>阶段</th>
                        <th>金额</th>
                        <th>操作</th>
                    </tr>
                </thead>
                <tbody>
                @if (Model == null || !Model.Any())
                {
                    <tr><td colspan="5" class="text-center">暂无数据</td></tr>
                }
                else
                {
                    foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Name</td>
                            <td>@item.AccountName</td>
                            <td>@item.Stage</td>
                            <td>@item.Amount.ToString("C")</td>
                            <td>
                                <a asp-area="CRM" asp-controller="Opportunities" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-info btn-sm">编辑</a>
                            </td>
                        </tr>
                    }
                }
                </tbody>
            </table>
        </div>
    </div>
</div>
